# Configuration (not secrets - keys go in SecretStore)

# Logging
LOG_LEVEL=INFO

# Model defaults (override per-run via config yaml)
DEFAULT_TEMPERATURE=0
DEFAULT_MAX_TOKENS=2048

# Paths (if non-standard)
# DATA_DIR=./domains/extraction/data
# RESULTS_DIR=./domains/extraction/results
